/**
 * Custom hooks for GTM Kit Settings
 *
 * Centralized export for all custom hooks. These hooks simplify
 * component code by reducing prop drilling and providing convenient
 * abstractions over contexts.
 *
 * @since 2026-01-27
 */

export { useSettingField } from './useSettingField';
export { useFeatureFlags } from './useFeatureFlags';
export { useNotification } from './useNotification';
